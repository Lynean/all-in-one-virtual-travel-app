[phases.setup]
nixPkgs = ['python3', 'pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.setup]
nixPkgs = ["python39"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn simple_server:app --host 0.0.0.0 --port $PORT"